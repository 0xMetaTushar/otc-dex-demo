/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://cdn.builder.io/api/v1/image/assets/TEMP/'],
  },
}

module.exports = nextConfig
